{% block nav %}
    <nav class="navbar navbar-fixed-top navbar-inverse">
        <div class="container-fluid">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="#">CYRIL TONNEVILLE</a>
            </div>

            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li class="active" id="liCV"><a href="#menuCV">CV <span class="sr-only">(current)</span></a></li>
                    <li id="liAboutMe"><a href="#menuAboutMe">{{ 'section.about_me'|trans|capitalize }}</a></li>
                    <li id="liEfficiencies"><a href="#menuEfficiencies">{{ 'section.efficiencies'|trans|capitalize }}</a></li>
                    <li id="liPortfolio"><a href="#menuPortfolio">{{ 'section.portfolio'|trans|capitalize }}</a></li>
                    <li id="liContactMe"><a href="#menuContactMe">{{ 'section.contact'|trans|capitalize }}</a></li>
                    <li id="liFollowMe"><a href="#menuFollowMe">{{ 'section.follow_me'|trans|capitalize }}</a></li>

                    {% if is_granted('ROLE_ADMIN') %}
                        <li><a href="{{ path('fos_user_security_login') }}">{{ 'administration'|trans|capitalize }}</a></li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>
{% endblock %}